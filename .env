API_KEY=#
API_SECRET=#

#Due to Binance Testnet API access issues (verification restriction), the project includes a fallback mock mode.
#The implementation is fully compatible with Binance Futures API and can be enabled by providing valid API credentials.git 